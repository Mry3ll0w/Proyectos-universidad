#include <stdio.h>
#include <stdlib.h>
/*Write a program that given an integer displays its 
decomposition into prime factors (every integer has a 
single decomposition given by a multiplication of 
powers of prime numbers). 
The program must calculate, not only these prime numbers, 
but also their associated power*/
int main(){
    int n,paso,vprimos[5]={2,3,5,7,11};
        printf("Introduce el numero\n");
            scanf("%i",&n);
}